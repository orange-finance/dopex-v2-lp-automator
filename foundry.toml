[profile.default]
src = 'contracts'
out = 'out'
libs = ['node_modules', 'lib']
test = 'test'
cache_path = 'cache_forge'
optimizer_runs = 200
ffi = true
fs_permissions = [{ access = "read", path = "./out" }]

[rpc_endpoints]
arb = "${ARB_RPC_URL}"

[invariant]
fail_on_revert = true
